# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Marketing site — optional webhook that receives each lead submitted
# through the SpeciaLevel contact form (Make.com / FormSubmit / Zapier).
# When unset, leads are stored locally in data/marketing-leads.jsonl.
MARKETING_LEADS_WEBHOOK_URL=
